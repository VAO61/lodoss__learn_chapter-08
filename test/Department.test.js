const Department = require('../app/department/Department');

describe('class Department', () => {
  let department = new Department();

  // describe('getUnBusyDevelopers', () => {
  //   test('some desc', () => {
  //     let developer1 = {
  //       skill: 1,
  //       projects: null,
  //       unBusyCount: 5
  //     };
  //     let developer2 = {
  //       skill: 2,
  //       projects: null,
  //       unBusyCount: 5
  //     };
  //     let developer3 = {
  //       skill: 3,
  //       projects: null,
  //       unBusyCount: 0
  //     };
  //     let developers = [{ developer1 }, { developer2 }, { developer3 }];
  //     for (let i = 0; i < developers.length; i++) {
  //       let getUnBusyDevelopers = developers.filter(
  //         developers[i].unBusyCount !== 0
  //       );
  //       expect(getUnBusyDevelopers.length).toBe(2);
  //     }
  //   });
  // });

  describe('', () => {
    test('', () => {
      // let developer = { skill: 1, projects: null, unBusyCount: 5 };
      // let developer1 = { skill: 1, projects: null, unBusyCount: 0 };
      // let developer2 = { skill: 2, projects: null, unBusyCount: 0 };
      // let developers = [{ developer }, { developer1 }, { developer2 }];
      // developers = developers.filter(item => {
      //   return item !== developer;
      // });
      // expect(department.statisticFiredDevelopers).toBe(1);
      // TODO: разобраться с входящими данными!
    });
  });
});
